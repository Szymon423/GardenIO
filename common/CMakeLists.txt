add_library(shared_memory src/shared_memory.cpp)
target_include_directories(shared_memory PUBLIC include)

add_library(signals_definitions INTERFACE)
target_include_directories(signals_definitions INTERFACE include)